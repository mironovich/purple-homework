apiVersion: v1
kind: Pod
metadata:
  name: converter
  labels:
    components: backend
spec:
  containers:
    - name: converter
      image: antonlarichev/conv-app:1.2
      ports:
        - containerPort: 80
      resources:
        limits:
          memory: 512Mi
          cpu: 500m
