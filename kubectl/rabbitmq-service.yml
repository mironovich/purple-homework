apiVersion: v1
kind: Service
metadata:
  namespace: converter-namespace
  name: rabbitmq-clusterip
spec:
  type: ClusterIP
  selector:
    components: rabbitmq
  ports:
    - port: 5672
      protocol: TCP
